<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>ULBC_IP_Hash__c</fullName>
    <description>SHA-256 hex of (ULBC_Email_Settings__c.IPHashSalt__c + recipient IP). Raw IPs are never stored (UK GDPR data minimisation). Same-day correlation works as long as the salt is unchanged; salt rotation makes historical IPs unrecoverable.</description>
    <externalId>false</externalId>
    <label>IP Hash</label>
    <length>64</length>
    <required>false</required>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
